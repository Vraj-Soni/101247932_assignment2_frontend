import React from "react";
const Header = () => {
    return (
        <nav className="navbar navbar-dark bg-dark">
            <span className="navbar-brand" style={{ color: '#fff' }}>Employee Management App</span>
        </nav>
    )
}

export default Header;